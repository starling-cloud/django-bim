---
applyTo: "tst/**/*.py"
---
<!-- generated by @starling-cloud/agents — edit fragments, not here -->
When editing tests:

- Use `pytest`; mirror the source layout under `tst/`.
- The contract test (`tst/test_contract.py`) is canonical — keep it passing and
  extend rather than weaken it.
- Tests must be deterministic: control env vars and time; don't depend on
  network or external services.
